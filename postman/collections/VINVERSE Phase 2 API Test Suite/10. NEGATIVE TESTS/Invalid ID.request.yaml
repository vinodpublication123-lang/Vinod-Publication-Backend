$kind: http-request
url: "{{baseUrl}}/products/invalid-id-format"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('Status code is 400 or 404', function () { pm.expect(pm.response.code).to.be.oneOf([400, 404]); });
    language: text/javascript
order: 4000
