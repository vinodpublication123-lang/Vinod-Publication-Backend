$kind: http-request
url: "{{baseUrl}}/admin/orders"
method: GET
headers:
  Content-Type: application/json
scripts:
  - type: afterResponse
    code: pm.test('401 no token', function() { pm.response.to.have.status(401); });
    language: text/javascript
order: 2000
