$kind: http-request
name: "[NEG] Missing Token — GET /users/me"
url: "{{baseUrl}}/users/me"
method: GET
headers:
  Content-Type: application/json
scripts:
  - type: afterResponse
    code: pm.test('401 Unauthorized', () => pm.response.to.have.status(401));
    language: text/javascript
order: 1000
